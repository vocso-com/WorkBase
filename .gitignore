node_modules
dist
target
.DS_Store

# Vite dependency pre-bundle cache
.vite

# Tauri auto-generated schemas (regenerated on every build)
src-tauri/gen/


# Local dev overrides (per-machine)
.env.local

# Secrets — never commit these. Keys go in the OS keychain or CI secrets.
.env
.env.*
!.env.example
*.pem
*.p12
*.key
*.keystore
*.mobileprovision

# Editor / assistant local config
.claude/
